{
  "name": "mern-ecommerce-fullstack",
  "version": "1.0.0",
  "description": "Full Stack MERN E-commerce platform with integrated client and server",
  "main": "backend/index.js",
  "scripts": {
    "build": "cd frontend && npm install --legacy-peer-deps && npm run build && cd ../backend && npm install",
    "start": "cd backend && node index.js"
  }
}
